# This hook is called after a new commit is pulled, but before that commit is bundled for re-use by Lit.

new_release_directory="$1"

composer install --no-interaction --no-progress --optimize-autoloader --no-dev

npm ci

npm run build

rm -rf "node_modules/"
rm -rf "tests/"
rm -rf "storage/"
